# n8n-with-yt-dlp
yt-dlp is a fast, open-source command-line downloader (a fork of youtube-dl) that can extract direct media URLs or download audio/video from YouTube, TikTok, Vimeo, etc. It supports tons of sites and, when paired with FFmpeg, it can merge audio+video or transcode.
